# 隐私说明（Privacy Policy）

补哩补哩（bulibuli）是一款本地运行的开源工具。本文说明它处理哪些用户数据、如何存储、发送到哪里，以及如何清除。最近更新：2026-08。

## 我们收集什么

本工具**不收集**任何遥测、统计数据或用户行为信息。唯一涉及用户凭据的数据是：

- **B 站 Cookie（含 `SESSDATA` 等登录凭据）**：由用户主动扫码或手动提供，用于访问 B 站官方接口（查询视频信息、历史记录、直播状态等）以及下载需要登录的媒体内容。
- **本地业务数据**：下载/监控任务、历史记录、会话与配对信息，全部保存在本机数据目录中。

## Cookie 存储在哪里

- Cookie 仅存储在**用户本地设备**上，位于程序数据目录 `data/` 内（SQLite 数据库及配置文件）。
- 落盘前按平台加密，加密方式因平台而异：
  - **Windows**：凭据直接由 **DPAPI**（当前用户作用域）加密，密钥由操作系统管理，不落盘于应用目录；
  - **macOS / Linux**：使用 **AES-256-GCM** 加密。主密钥 macOS 存入 **Keychain**；Linux 默认以 `0o600` 权限文件存放于数据目录，同时支持通过环境变量 `BILI__MASTER_KEY` 注入外部主密钥（适合服务器/容器部署自行管理密钥）。
- Cookie 永远不会出现在日志中（见下文"日志脱敏"）。

## 数据发往哪里

- 本工具**不回传任何数据给开发者或任何第三方服务器**。
- 所有携带 Cookie 的网络请求**仅发往 bilibili 官方域名**（如 `bilibili.com`、`bilibili.tv` 及其 API 子域），用于实现用户主动发起的功能。
- 下载流量来自 B 站 CDN 与用户在设置中信任的本地 aria2 实例；应用内更新检查仅访问本项目的 GitHub Releases 页面。
- GeoIP 功能使用本地内置数据库离线判断 IP 归属，不做在线查询。

## 如何清除数据

- **登出 B 站账号**：在网页设置的凭据管理中删除对应 Cookie 记录，加密条目即被移除。
- **彻底清除**：停止程序后删除整个 `data/` 目录（默认位于可执行文件旁；自定义部署见 `BILI__DATA_DIR`）。该目录包含数据库、Cookie、会话、配对状态和下载文件，删除后不可恢复。
- 卸载程序只需删除安装目录与 `data/` 目录，不在系统其他位置留下副本。

## 日志脱敏

- 日志中不会输出完整 Cookie、`SESSDATA`、令牌或配对码；相关字段在写入前会被截断或替换为占位符。
- 尽管如此，日志可能包含本地路径与错误详情，提交 issue 前请按 [SECURITY.md](SECURITY.md) 的要求自行脱敏，不要上传日志、数据库或整个 `data/` 目录。

## 相关文档

- [安全策略](SECURITY.md)
- [部署安全说明](deploy/SECURITY.md)
- [第三方资源与许可](NOTICE.md)
