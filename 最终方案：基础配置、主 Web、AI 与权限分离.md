# 最终方案：基础配置、主 Web、AI 与权限分离

这个方案可行，但 AI 配置高权限内容不能等同于普通 AI Skill。应增加“人类明确授权的 AI 基础配置模式”。

## 一、四个入口的最终职责

| 入口         | 作用                                                        |
| ------------ | ----------------------------------------------------------- |
| 终端 TUI     | 人类查看提示、执行 `help`、运行基础 `setup`、处理服务端问题 |
| 基础配置 Web | 人类配置会影响程序运行和安全的基础设置                      |
| 主 Web       | 人类进行账号、下载、UP 主和业务操作                         |
| `ctl`        | AI 专用接口，可执行多个业务操作和规则配置                   |

终端 `setup` 与基础配置 Web 是同一套基础配置的两种入口，不再复制主 Web 功能。

## 二、基础配置范围

基础配置只保留真正会影响程序运行或安全边界的内容：

- 监听模式、端口、IPv4/IPv6、代理域名
- IP 访问规则、GeoIP 限制
- 数据根目录和下载根目录
- Aria2 内置/外部模式
- 外部 Aria2 地址、端口和信任关系
- FFmpeg 模式和可信执行路径
- 程序级并发、资源和运行限制
- 初始配对和设备会话
- AI Skill 开关
- 配置摘要、配置校验、重启提示

基础配置 Web 不显示：

- 下载任务
- UP 主监控
- 弹幕和字幕偏好
- 历史记录
- 普通下载规则
- 主 Web 的业务设置

## 三、主 Web 要移除的重复内容

主 Web 当前设置页中，以下内容应移到基础配置 Web或仅保留只读摘要：

- 网络访问模式
- 代理域名
- IP 访问策略
- GeoIP
- AI Skill 开关
- 数据目录
- Aria2 模式和外部连接配置
- FFmpeg 可执行路径
- 设备配对和会话管理

主 Web 中只显示：

```text
基础配置状态：正常
AI Skill：已启用
当前访问模式：IPv6/代理
基础配置入口：可访问 / 仅服务端可访问
```

不重复编辑这些字段。

## 四、主 Web 保留的内容

主 Web 专注业务和账号：

- B站扫码登录
- 手动粘贴 Cookie
- 账号切换和退出登录
- 下载任务
- UP 主管理
- 监控规则
- 视频质量和音频偏好
- 下载路径模板
- 弹幕、评论、字幕
- 智能下载
- 多 P 下载
- 失败重试和优先级
- 历史、日志、看板
- 文件保留和校验
- 业务规则

Cookie 和 B站登录必须由人类在主 Web完成。

## 五、主 Web 的权限分级

为避免主 Web 暴露后把高权限一起开放，增加会话角色：

| 角色        | 可用内容                             |
| ----------- | ------------------------------------ |
| Owner/Admin | 主 Web全部功能、B站登录、账号管理    |
| Operator    | 查看状态、添加任务、管理普通业务规则 |
| Viewer      | 只读查看                             |

Operator 和 Viewer 看不到：

- Cookie 输入框
- B站账号登录按钮
- AI Skill 开关
- 数据目录
- 网络配置
- Aria2/FFmpeg 信任路径
- 基础配置入口

首次配对生成 Owner 会话；Owner 可以生成限时 Operator 邀请。

这样可以把主 Web 开放到 IPv6，同时不把基础配置和账号权限开放给普通使用者。

## 六、AI 的正常权限

AI 通过 `ctl` 执行多个命令，默认可以：

- 查询状态
- 查询登录状态，但不能读取 Cookie
- 批量添加、暂停、恢复、重试、删除任务
- 搜索、添加和管理 UP 主
- 创建、修改、删除监控规则
- 配置自动下载规则
- 修改视频质量、下载模板、重试、保留数量等业务设置
- 执行批量刷新和状态分析
- 读取审计记录

AI 使用结构化修改接口：

```text
ctl settings get
ctl settings set <key> <value>
ctl rule create
ctl rule update
ctl rule delete
ctl batch ...
```

不允许 AI 直接提交完整设置对象，避免覆盖人类已经配置好的内容。

## 七、AI 配置高权限基础设置

对于以下操作，AI 可以辅助执行，但必须进入临时授权流程：

- 生成配对码
- 修改 IP 访问规则
- 设置代理域名
- 配置 GeoIP
- 修改 Aria2 信任地址
- 添加 FFmpeg 可信目录或路径
- 修改数据目录
- 调整公网访问模式

### 推荐流程

1. AI 先生成配置计划：

```text
准备执行：
- 将访问模式改为 proxy
- 设置域名 downloads.example.com
- 允许 IPv6 地址段 2001:db8::/32
- 添加 FFmpeg 路径 C:\tools\ffmpeg.exe

风险：
这些设置会影响远程访问和程序安全。
```

2. 用户在终端 TUI 或基础配置 Web 确认。

3. 后端生成短期授权令牌。

4. AI 使用授权令牌执行一组操作。

5. 所有修改写入审计日志，并显示变更前后差异。

AI 不需要用户理解每个字段，但用户至少需要确认“允许 AI 修改基础配置”。

可以提供一个简单的人类命令：

```text
ai assist on
```

或基础配置 Web 中的：

```text
临时允许 AI 协助配置基础设置
```

授权默认只持续一次任务或几分钟，完成后自动关闭。

## 八、AI 遇到扫码和 Cookie

AI 不直接处理 Cookie，也不代替扫码。

AI 应返回：

```text
请打开主 Web → B站账号登录 → 使用 B站 App 扫码。
完成后回复“已扫码”，我会继续检查登录状态。
```

AI 可以：

- 打开或准备登录流程
- 生成主 Web 地址
- 检查是否已经登录
- 扫码完成后继续执行后续任务

AI 不能：

- 读取 Cookie 原文
- 输出 Cookie
- 代替用户确认登录
- 自动切换账号

## 九、基础配置无法从主 Web访问时

主 Web 不强行跳转基础配置。

如果检测到基础配置 Web不可访问，显示：

```text
基础配置当前仅允许服务端访问。

请在服务器后端 TUI 输入：
setup

也可以通过 SSH 隧道访问基础配置 Web。
```

如果基础配置 Web可访问，则显示“打开基础配置”按钮。

基础配置服务的状态需要返回：

```text
local_only
remote_available
unavailable
```

主 Web根据状态显示按钮或后端 TUI 提示，避免出现打不开的死链接。

## 十、云服务器方案

- 本机部署：基础配置 Web继续监听本机。
- 云服务器：默认使用 SSH 隧道。
- 需要远程初始化时，提供短期 setup URL/token。
- setup token 只允许基础配置，不能访问主 Web业务。
- 通过 HTTPS 反向代理后，用户可从 IPv6访问主 Web。
- 基础配置仍可只保留在服务端或 SSH 隧道中。
- AI可以生成 SSH、Caddy、Nginx 配置并引导用户操作。

## 实施重点

- 将基础配置从主 Web设置页拆出。
- 终端 `setup` 与基础配置 Web共用字段和校验。
- 主 Web保留 B站登录，但增加 Owner 权限限制。
- 增加 Owner、Operator、Viewer 会话角色。
- `ctl` 保持 AI 专用、支持批量操作。
- 增加 AI 基础配置计划、确认、临时授权和审计流程。
- 主 Web根据基础配置 Web是否可达显示不同提示。
- 不再存在“setup、基础配置 Web、主 Web重复编辑同一个设置”的问题。
