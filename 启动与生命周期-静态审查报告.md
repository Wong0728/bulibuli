# BuliBuli 全项目静态审查报告

## 1. 审查说明

- 审查日期：2026-08-24
- 审查仓库：`D:\codework\BuliBuli_0813`
- 分支：`main`
- Git 基线 HEAD：`de1b7dc956f83237d110bc9960561ed2a6c31e8e`
- 审查类型：源码、配置、路由、文档和构建脚本的本地静态审查
- 代码变更：无；本文件是本轮唯一新增文件

用户提供的 `C:\Users\WongJY\Desktop\1. 启动与生命周期.md` 是审查范围清单。文档本身明确说明其中内容“只是排查范围，不代表这些地方已经确认有问题”，因此本报告把它作为检查维度，不把其中的检查项直接当作缺陷结论。用户的实际要求是：先完成 Git 基线检查，再按这些方面进行完整审查并把结果写入 Markdown，不修改代码。

本轮没有启动服务、浏览器、构建、测试、真实网络请求或下载，也没有修改、回退、清理已有工作区改动。因此，“确认”均表示静态代码证据；不能等同于运行时复现或生产可用性证明。

## 2. Git 基线与工作区边界

审查开始前工作区状态为：

```text
## main
 M Cargo.lock
 M Cargo.toml
 M src/api/setup.rs
 M src/app/setup_server.rs
 M src/app/term_style.rs
 M src/app/tui.rs
 M src/db/init.rs
 M src/main.rs
 M src/state/infra.rs
 M web/src/api/index.ts
 M web/src/api/types.ts
```

基线统计为 11 个已跟踪修改文件，约 253 行新增、80 行删除；没有未跟踪文件。上述改动属于审查开始前已有内容，本轮未执行 `reset`、`checkout`、清理或覆盖。报告生成后会新增本文件，除此之外不应出现新的源码改动。

## 3. 总体结论

本轮发现 4 类需要优先处理的静态问题，以及若干需要明确边界的架构风险：

| 优先级 | 结论 | 静态判定 |
|---|---|---|
| S1 | Setup 完成后关闭 Setup 端口，但前端没有稳定切换到主端口 | 已确认存在跨端口生命周期/路由缺口 |
| S1 | 应用内数据库备份不是完整用户恢复备份 | 已确认备份范围不包含若干关键状态和密钥材料 |
| S2 | 主端口 fallback 和 LAN 地址的展示存在时序/地址误导 | 已确认静态风险；fallback 窗口需运行验证 |
| S2 | 后台任务没有统一的取消、JoinHandle 保存和退出等待 | 已确认若干任务脱离统一等待链；实际退出竞态未运行复现 |
| S2 | 凭据可能出现在错误日志或 aria2 命令行参数中 | 已确认暴露面，是否被特定部署实际读取需运行验证 |
| S2 | aria2 子进程强制回收的跨平台覆盖不对称 | 已确认实现边界，macOS/Android 等平台未做运行验证 |
| S3 | 普通配置、`security.toml`、数据库 runtime 配置、`startup_state.json` 分属不同真相源 | 架构边界需文档化，不宜直接认定为单一 Bug |

同时，数据库初始化、认证/CSRF、文件安全、下载 generation 守卫、直播健康状态、弹幕解析限制、前端错误 envelope 和包校验等方面存在较好的静态防护。但这些正面证据不覆盖真实 B 站、aria2、FFmpeg、浏览器、多客户端并发和不同平台产物运行。

## 4. 关键问题详述

### 4.1 S1：Setup 完成后的跨端口切换不闭合

证据链如下：

1. `src/main.rs:134-165` 先启动独立 Setup 服务并运行 onboarding，主端口 listener 到 `src/main.rs:192` 才绑定。
2. `src/app/setup_server.rs:105-134` 的 Setup 端口只挂载静态页面、auth、health 和 `setup::router()`。
3. 主端口的完整 API 在 `src/api/mod.rs:42-59`，包括 `/api/cookies/status`、下载、历史、设置、直播、更新等；这些业务路由没有挂到 Setup 端口。
4. `src/app/setup_server.rs:219-228` 在 `/api/setup/apply` 成功、onboarding 已完成且没有设置 `BILI__SETUP_PORT_ENABLED=true` 时调用 `shutdown_setup_server()`。
5. `web/src/stores/setup.ts:56-70` 在 `apply` 返回后还会重新读取 Setup 状态；`web/src/views/SetupView.vue:43-64` 随后还会保存 AI Skill、再次读取状态并发出 `done`。
6. `web/src/App.vue:96-113,125-133` 的 `resolveEntryPhase()` 和 `onSetupDone()` 继续使用同源相对路径；`web/src/api/client.ts:7,119-126` 没有主端口切换逻辑。

因此，默认模式下成功提交后，浏览器仍停留在 Setup 端口，而该端口正在被关闭或已经关闭。后续请求可能表现为连接失败；即使 Setup 端口尚未完全退出，请求 `/api/cookies/status` 等主端口业务路由也会得到路由不存在。若选择的网络模式需要重启，后端返回了 `restart_required`，但当前流程同样没有形成“明确提示、等待重启、重新定位主端口”的闭环。

这不是单一错误提示问题，而是“提交响应、服务关闭、前端状态推进、主端口接管”四者之间缺少协议。建议后续把端口交接设计成明确契约：由后端返回可访问的主端口/地址，由前端在响应可消费后切换；或者在主端口确认可用前继续保留必要的 Setup 路由。该建议本轮不实施。

### 4.2 S1：应用内备份不能独立完成用户状态恢复

`src/api/backup.rs:25-56` 的 `/api/backup` 只执行 SQLite `VACUUM INTO`，并轮转 `data/backups/bulibuli-backup-*.db` 最近 5 份。数据库快照本身是有价值的一致性备份，但以下关键内容不在该 API 的输出中：

- `src/services/secret_store.rs:3-14,43-63` 使用的主密钥来源和密钥材料：Linux/Unix 可能是 `data/.secret-store.key`，Windows 使用 DPAPI，macOS 使用 Keychain，或使用 `BILI__MASTER_KEY` 环境变量。
- `src/services/security_config.rs:191-204` 读取和写入的 `data/security.toml`。
- `src/app/onboarding.rs:6-18,60-73` 使用的 `startup_state.json`。
- 下载目录、日志、迁移副本、运行状态和其他 data 目录内容。

README 已经在 `README.md:164-169` 要求升级前停止程序并备份整个 `data/`，部署安全文档也在 `deploy/SECURITY.md:61-69` 要求完整备份。这说明“完整 data 目录备份”是当前运维恢复边界；但用户容易把 `/api/backup` 理解为应用内完整备份。建议明确区分“数据库快照”和“可恢复备份”，并给出密钥/系统密钥环/环境变量的恢复要求。否则仅恢复 `.db` 可能导致密文 Cookie 和 aria2 secret 无法解密，或丢失访问模式与 onboarding 状态。

### 4.3 S2：实际主端口存在初始化窗口，Setup 页面可能显示错误端口

`src/state/infra.rs:76-78` 创建 `InfraState` 时把 `actual_main_port` 初始化为配置端口；真正 listener 在 `src/app/security_server.rs:106-134` 绑定完成后才写入实际端口。另一方面，`src/main.rs:134-165` 启动 Setup 服务早于 `src/main.rs:192` 的主端口绑定，`src/api/setup.rs:43-81` 会直接读取 `actual_main_port` 并生成 `main_url`。

当配置端口被占用、主端口最终 fallback 到后续端口时，Setup 服务在主端口绑定前可能短暂返回配置端口，而非真实 listener 端口。代码在 `src/app/security_server.rs:106-134` 已正确把绑定后的端口写入 `actual_port.txt`，但这不能消除前面的读取窗口。建议后续把“未绑定”表示为 0/未就绪，或在主端口绑定后再发布 Setup 的可用状态。该窗口尚未通过端口占用运行场景复现。

### 4.4 S2：LAN 模式的主 URL 仍显示 127.0.0.1

`src/app/security_server.rs:79-98` 的 `main_url()` 在 `AccessMode::Local | AccessMode::Lan` 两种模式都生成 `http://127.0.0.1:{port}`。`src/api/setup.rs:43-81` 另行计算了 LAN 下的 `accessible_urls`，能够列出本机 IPv4/IPv6 地址；但 `src/main.rs:198-214` 的 TUI/启动提示使用的是 `main_url`，不是 `accessible_urls`。

结果是服务器本机看到的地址可用，但局域网其他设备若照着启动提示或主 URL 访问，会被引导回它们自己的回环地址。LAN 模式的 `accessible_urls` 是正面补救，但入口展示没有统一使用它。该问题不等同于 LAN 监听失败；监听策略本身在 `src/app/security_server.rs:121-134` 使用 `[::]`，实际可达性仍需网络环境验证。

### 4.5 S2：后台任务的退出等待不统一

主流程在 `src/main.rs:226-243` 取消共享 token 后，显式等待下载监控、普通监控、刷新、直播监控、B 站验证服务、录制和 aria2，再关闭数据库。这部分顺序是正面的，但静态代码还存在未纳入同一 Join 链的任务：

- `src/app/control.rs:299-325` 的 IPC server/stdin loop 使用 `tokio::spawn`，主流程不保存 JoinHandle。
- `src/main.rs:125-130` 的启动更新检查使用 `tokio::spawn`，没有保存句柄。
- `src/app/setup_server.rs:83-91` 的 Setup server 使用 `tokio::spawn`，只通过独立 token 优雅退出，主流程不等待其任务完成。
- `src/ws/mod.rs:132-162` 的 audit event bridge 使用 `tokio::spawn`，函数签名没有接收 cancellation token，循环主要依赖 broadcast channel 关闭；注释声称“随 cancellation 取消退出”，但代码没有对应的 `select!` 取消分支。
- `src/services/monitor.rs:116-131` 的 `monitor_backfill_fans` 另行 `spawn_logged`，没有返回给 `stop()` 等待。
- `src/services/download/monitor.rs:343-355` 的磁盘恢复自动续传也会单独 spawn。
- `src/app/control.rs:2745-2783` 还有控制路径下的异步任务创建。

这意味着数据库关闭时，部分 detached task 仍可能处于访问共享状态的窗口。不能仅凭静态代码断言必然出现数据库关闭错误，但生命周期所有权不完整，应该后续按任务分类建立取消信号、JoinHandle 归属和退出超时策略。

### 4.6 S2：凭据泄露暴露面

两处静态暴露面需要单独处理：

- `src/services/settings.rs:547-553` 在 `runtime_config` JSON/TOML 解析失败时记录原始内容前 256 字节。若该配置 blob 包含 aria2 secret、代理凭据或其他敏感字段，截断日志仍可能泄露秘密。当前代码只说明“前 256 字节”，没有字段级脱敏。
- `src/services/aria2/process.rs:167-169` 把 RPC secret 作为 `--rpc-secret=...` 子进程命令行参数。某些系统/权限模型允许同机进程或诊断工具读取命令行，命令行参数不是可靠的秘密存储边界。

`SecretStore` 本身已明确记录 Linux 文件密钥、Windows DPAPI、macOS Keychain 和 `BILI__MASTER_KEY` 的威胁模型，这是正面证据；本问题是日志/进程启动面的额外暴露，不是断言存储模块使用明文数据库。

### 4.7 S2：aria2 子进程回收的跨平台边界

`src/services/aria2/process.rs:179-203,246-309` 有 `kill_on_drop`、优雅停止、超时强杀以及 Windows Job Object；Linux 还有 parent-death 相关处理。这些措施覆盖良好，但当前实现的强制父子进程绑定并不对所有目标平台对称，尤其 macOS、Android/Termux 等平台的“父进程被强制结束后 aria2 是否一定退出、会话文件是否完整”不能从 Windows Job Object 逻辑推导出来。应将这些平台场景列入发布前运行验证，而不是把静态存在的 `kill` 当成全部平台的孤儿进程证明。

## 5. 按附件 16 个方面的审查结果

### 5.1 启动与生命周期

启动顺序大体为：配置/路径、实例锁、数据库、AppState、IPC、更新检查、Setup、onboarding、审计桥、后台服务、主端口绑定、TUI/stdin、主服务。`src/main.rs:109-226` 显示主端口在后台服务启动后才绑定，能减少“端口已监听但依赖服务尚未就绪”的假响应窗口；实例锁也在 `src/main.rs` 相关启动路径中持有。

已发现的端口窗口、LAN 地址和 Setup 交接问题见第 4 节。端口占用、权限不足、依赖缺失、强制结束后的实际恢复未运行验证；数据库关闭前的 detached task 风险见 4.5。

### 5.2 配置与环境

`src/config.rs:226-265` 的普通配置路径是“默认值 → `BILI__*` 环境变量 → 校验 → 路径创建”。普通配置没有统一读取一个常规配置文件；安全模式由 `data/security.toml` 管理，运行时业务设置由数据库中的 `runtime_config` 管理，onboarding/终端状态由 `startup_state.json` 管理。这样的分层可以成立，但“哪个值优先、哪些变更立即生效、哪些必须重启”需要成为公开契约。

已有边界包括数据目录绝对路径校验和目录创建；`security.toml` 损坏会报配置错误，`runtime_config` 损坏会记录错误并回退默认配置，`startup_state.json` 损坏会回退默认状态并重新进入向导。相对路径、中文路径、空路径、非法路径、环境变量变更后的重启行为仍没有运行验证；runtime 配置日志的敏感信息问题见 4.6。

### 5.3 API 契约与路由

正面证据：

- `src/api/mod.rs:42-59` 集中挂载主端口业务路由。
- `src/error.rs` 统一错误 envelope 和 HTTP 状态映射；`web/src/api/client.ts:91-204` 处理非 JSON、HTTP 错误、envelope 错误、401/403、网络失败和超时。
- `web/src/stores/setup.ts:8-15` 对 Setup 返回结构有明确类型约定，当前字段与 `src/api/setup.rs:25-81` 的结构基本对应。

主要缺口是主端口与 Setup 端口不是等价路由集合：Setup 端口只有 auth、health、setup，前端却在 Setup 完成后的同一页面继续访问主端口业务 API。该缺口与 4.1 是同一个跨层问题。没有进行全量自动接口矩阵、真实 HTTP 状态和所有前端类型的运行校验。

### 5.4 认证与安全边界

静态实现覆盖了 session TTL/轮换、角色权限、CSRF、Host/Origin 检查、配对限流和 Setup 回环 Host 限制；`src/app/setup_server.rs:137-218` 还为 Setup 端口补入 `ClientInfo`/Session，并将写请求 Origin 绑定到完整 authority。`src/services/file_safety.rs`、`src/services/bili_url_policy.rs` 对下载 URL、目录、文件名、DNS/私网访问有校验。

需要注意：LAN 使用 HTTP，部署文档已说明它只适合可信局域网；IP/CIDR 规则不能替代主机防火墙。备份、日志和 aria2 命令行的凭据边界见 4.2 和 4.6。未验证真实代理头、IPv6、反向代理、配对暴力尝试和不同角色的运行行为。

### 5.5 数据一致性与恢复

`src/db/init.rs:12-62` 配置了 SQLite WAL、busy timeout、外键和迁移前备份/失败恢复；`src/db/init.rs:65-92` 使用 `seaql_migrations.version` 查询迁移版本，避免把迁移表的其他列误当版本。下载状态使用 generation guard，文件归位使用 SHA-256 去重，能降低陈旧回调覆盖新状态的风险。

已确认的恢复缺口是 `/api/backup` 只做数据库快照，完整 data/密钥恢复要求见 4.2。写数据库与写文件中途崩溃、磁盘满、文件被占用、删除历史是否留下敏感残留，静态检查未能证明完整正确，需要故障注入和恢复测试。

### 5.6 异步并发与任务状态机

`src/services/download_state.rs`、`src/services/download/queue.rs`、`src/services/download/completion.rs` 提供重复任务串行化、generation 守卫、SHA-256 去重和完成管线；下载重试也有 backoff 结构。监控服务有显式 handle 和 stop 超时，直播监控有 heartbeat/last-success/last-error/stale 状态及风险退避。

主要静态风险不是“重复任务必然发生”，而是后台任务所有权不统一，详见 4.5。channel 满载、死锁、阻塞操作占用 Tokio worker、无限重试和同时暂停/恢复/删除的真实交错顺序仍未运行验证。

### 5.7 Vue 前端状态机

`web/src/App.vue:82-133` 和 `web/src/stores/app.ts:441-465` 明确区分 `loading`、`pair`、`setup`、`bili-login`、`main`、`unavailable`；`web/src/api/client.ts` 和 app store 对网络失败、401、403、风险控制和 stale/error 展示有统一入口。前端也有设备 session 失效后回到配对流程的处理。

Setup 完成后的同源 URL 与 Setup 端口关闭问题是已确认缺陷，详见 4.1。页面刷新、后退、多标签、真实 session 过期和浏览器卸载时请求取消没有通过浏览器验证；`web/` 到 `static/app` 的产物同步只做了静态路径核对。

### 5.8 WebSocket、IPC 与 TUI

IPC 命令有来源门控和 AI Skill 开关；`src/app/control.rs` 中 `COMMAND_REGISTRY` 还是 `docs/skill.md` 的代码来源，并有同步测试。WebSocket 审计事件桥对序列化失败、客户端广播失败和 broadcast lag 有日志处理；弹幕、下载进度等流式路径存在大小/事件约束。

审计桥的 cancellation 注释与实现不一致，且未被主流程等待，见 4.5。TUI 使用的 URL 可能是 LAN 下的 127.0.0.1，见 4.4。非 UTF-8、长日志、管道断开、终端窗口关闭和客户端异常断开仅有静态处理线索，未做交互验证。

### 5.9 B 站及其他外部服务

代码中存在 B 站请求超时、错误分类、Cookie 状态分类、WBI/登录相关服务和前端慢端点超时；aria2/FFmpeg 有超时、优雅退出和部分强杀边界；更新逻辑有下载、校验、暂存和失败保留旧文件的静态路径。

本轮没有访问 B 站、GitHub、aria2、FFmpeg 或 FFprobe。因此风控、Cookie 过期、WBI 字段变化、CDN 轮换、真实 JSON-RPC 重启、FFmpeg 版本差异、GitHub 不可用和下载中断都属于未验证项，不能把静态超时/重试代码当作端到端证明。

### 5.10 下载与媒体处理

下载队列、监控和完成管线使用任务 generation 防止陈旧状态写回；队列有重复任务处理和 backoff；`src/services/download/storage.rs` 与 history sync 使用流式 SHA-256 做去重和归位。`file_safety` 与 `bili_url_policy` 对路径穿越、文件名、下载目录和 URL/私网访问提供静态限制。

单 P、多 P、番剧、音频、弹幕、评论、字幕、合并/转码失败后的残留文件，以及磁盘满、权限不足、路径过长和部分文件恢复没有运行验证。不能由哈希去重直接推出“数据库记录和实际文件始终一致”。

### 5.11 直播录制与弹幕

`src/services/live_monitor.rs` 维护 heartbeat、last success、last error、stale/risk 状态和退避；直播录制 FFmpeg session 有超时和进程回收；弹幕 collector 有重连退避、协议解压大小限制和嵌套深度限制。这些是明确的正面静态证据。

真实开播、断流、假离线、流地址刷新、FFmpeg 卡死/强杀、分段合并、崩溃恢复、弹幕鉴权和事件顺序未通过真实直播或 Mock 运行验证。

### 5.12 平台兼容性

SecretStore 对 Windows DPAPI、macOS Keychain、Linux/其他 Unix 文件密钥有分支；aria2 对 Windows Job Object 和 Linux 父进程死亡有处理；构建/发布脚本覆盖 Windows、Linux、Termux 目标，并有路径和清单逻辑。

当前静态风险是子进程回收、信号、权限、shell quoting、IPv4/IPv6 和系统依赖选择在不同平台不完全等价，详见 4.7。没有在 Windows、Linux、macOS、Termux 的真实安装产物中启动、升级、强杀和恢复。

### 5.13 构建、打包、安装和更新

`web/vite.config.ts:5-29` 和 `build.py:193-227,326-360` 统一使用 `web/ → static/app/`，发布工作流生成包清单与 SHA-256，README/安装器保留 `data/`。部署文档明确记录端口 fallback 后使用日志或 `data/actual_port.txt` 更新反代目标，安装更新也有失败保留旧版本的说明。

本轮没有运行 Node/Vite、Rust、Python 构建、安装器、归档校验或任何发布产物。因此“有包清单/有 checksum”不等于包能在各目标系统正常启动；也没有证明升级时程序占用、旧进程、回滚和 data 保留路径在真实环境有效。

### 5.14 测试与验证边界

附件要求的新安装、升级、正常重启、强制结束、端口占用、网络断开、外部服务异常、磁盘满、权限不足、多客户端、角色和多平台场景，本轮均未执行动态测试。应在后续把测试结果按以下层级记录：

| 层级 | 本轮状态 |
|---|---|
| 静态源码/配置审查 | 已完成本报告范围 |
| 单元/集成测试 | 未运行 |
| 本地 Mock/故障注入 | 未运行 |
| 浏览器运行测试 | 未运行 |
| 真实 B 站、aria2、FFmpeg 端到端 | 未运行 |
| 多平台安装/升级/强杀恢复 | 未运行 |

### 5.15 性能与资源

代码中能看到部分边界措施：认证限流 key 有 `MAX_RATE_LIMIT_KEYS` 和清理间隔；下载 backoff 会删除旧键；WebSocket/broadcast lag 会报告丢弃数量；文件哈希采用流式读取而不是把大文件整体载入内存；前端慢端点有差异化超时。

仍未证明连接池、WebSocket 客户端、日志、缓存、临时文件、文件句柄和进程在长时间高负载下有界。detached task、失败重试放大和启动更新检查的资源占用需要压力测试与退出监控。

### 5.16 文档、运维和用户体验

README、`deploy/SECURITY.md` 和安装脚本对 data 目录、升级、端口 fallback、LAN 明文限制、备份和 SHA-256 有较清晰说明；错误 envelope 和前端错误分层也有较好的用户可见性。

需要统一的用户路径包括：

- Setup 完成后究竟访问哪个端口，以及什么时候切换到主端口。
- LAN 模式应该展示本机回环 URL 还是每个可访问地址。
- `/api/backup` 是数据库快照还是完整恢复备份。
- `restart_required` 时用户是否必须重启，重启后如何重新定位 URL。
- 错误日志、备份、命令行和 issue 收集中的凭据脱敏边界。

## 6. 用户角色 × 入口 × 生命周期覆盖矩阵

| 维度 | 静态覆盖结论 | 仍需动态验证 |
|---|---|---|
| 未配对用户 | Setup/auth/health 路由与配对流程存在 | 首次启动、Setup 关闭和配对码过期 |
| Owner | 主端口业务、备份、Cookie/设置等路由有权限链 | 真实 owner 操作与 401/403 恢复 |
| Operator/Viewer | 认证服务和命令门控存在 | 每个业务接口的最小权限矩阵 |
| AI Skill | `ai_skill_enabled` 和 ctl 来源门控存在，文档有同步测试 | 真实 AI/IPC 调用、长命令、退出过程 |
| Local | 主端口回环绑定与 Host/Origin 检查存在 | 浏览器与端口冲突场景 |
| LAN | `[::]` 监听、IP/CIDR 和 accessible URLs 逻辑存在 | IPv4/IPv6、局域网设备、HTTP 风险边界 |
| Proxy | proxy 域名/TLS 校验与反代文档存在 | Caddy/Cloudflare/转发头/真实 TLS |
| 首次启动 | DB → Setup → onboarding → 主服务的静态顺序可追踪 | Setup 完成后端口交接 |
| 正常重启 | startup state、security.toml、数据库加载路径存在 | 真实重启后数据与端口恢复 |
| 异常退出/强杀 | 部分服务有取消、超时、Job Object 和迁移备份 | detached task、aria2/FFmpeg、WAL 和文件恢复 |
| 升级 | 安装器/脚本保留 data，包有清单和 checksum | 占用、失败回滚、跨平台升级产物 |

矩阵说明了本轮的主要边界：认证和路由结构可以静态追踪，但“所有角色、所有入口、所有生命周期”仍不能只靠源代码证明。

## 7. 建议的后续修复优先级（本轮不实施）

1. 先修复 Setup 端口交接协议：处理 apply 响应、AI Skill 保存、页面状态推进、主端口地址切换和 `restart_required`，并覆盖 Setup 端口自动关闭/常驻两种模式。
2. 明确实际端口发布时序：避免在 listener 尚未绑定时把配置端口当成实际端口返回；LAN 展示统一使用可访问地址。
3. 把数据库快照与完整 data/凭据恢复分别命名、分别说明，并补充密钥环/环境变量恢复要求。
4. 建立后台任务清单：每个 spawn 明确 owner、CancellationToken、JoinHandle、退出超时和数据库关闭前的等待顺序；修正审计桥注释与实现不一致的问题。
5. 对 runtime 配置日志做字段级脱敏，避免把 RPC secret 放入可被同机读取的命令行参数。
6. 为 Windows、Linux、macOS、Termux 增加实际启动、端口占用、强杀、升级、aria2/FFmpeg 回收和恢复验证，并在发布记录中区分静态、Mock、浏览器和真实外部服务证据。

## 8. 最终边界声明

本报告完成的是针对附件 16 个方面的源码级审查与证据整理，不是运行验收、渗透测试、真实 B 站兼容性证明或多平台发布认证。报告没有修改现有代码，也没有把静态存在的防护机制描述成已经通过真实场景验证。最需要优先处理的是 Setup 跨端口交接和备份恢复边界；其余风险按第 4、5、7 节的优先级继续推进即可。

## 9. 本轮按报告实施的修复记录（2026-08-24）

本节记录报告生成后按第 7 节执行的代码修复；第 1—8 节保留为修复前的静态审查基线。

| 报告问题 | 修复结果 | 证据 |
|---|---|---|
| Setup 完成后的跨端口交接 | 已修复：`apply` 不再抢先关闭 Setup；前端先保存 AI Skill，再消费主端口 URL，调用 `/api/setup/finish` 后跳转；主端口未就绪时轮询 Setup 端口，`restart_required` 保留可访问主 URL | `src/api/setup.rs`、`src/app/setup_server.rs`、`web/src/views/SetupView.vue`、`web/src/App.vue` |
| 实际端口 fallback / LAN 地址误导 | 已修复：`actual_main_port`/`actual_setup_port` 未绑定时为 0；Setup API 返回空 URL 而不是配置假端口；LAN 主 URL 优先使用本机可访问地址，仍返回完整地址列表 | `src/state/infra.rs`、`src/app/security_server.rs`、`src/api/setup.rs` |
| 备份恢复边界 | 已修复并明确命名：`/api/backup` 是数据库快照；新增 Owner-only `/api/backup/full`，生成带清单的完整恢复目录，复制状态、密钥材料、日志和下载文件并排除 live DB/备份根目录 | `src/api/backup.rs`、`src/app/security_server.rs`、`README.md`、`deploy/SECURITY.md` |
| 后台任务退出所有权 | 已修复报告列出的 detached 链：IPC/stdin、Setup、启动更新、审计桥/清理、监控补齐、磁盘恢复续传均有 cancellation/JoinHandle/JoinSet，并在数据库关闭前等待 | `src/main.rs`、`src/app/control.rs`、`src/ws/mod.rs`、`src/services/{audit_log,monitor,download/*}.rs` |
| 凭据暴露面 | 已修复：runtime 配置解析失败不再记录原始内容；aria2 RPC secret 改由短生命周期 0600 临时配置文件读取，命令行不再出现 secret | `src/services/settings.rs`、`src/services/aria2/process.rs` |
| 跨平台 aria2 回收 | 已补强并文档化：统一加入 `--stop-with-process`，保留 Windows Job Object、Linux pdeathsig 和正常 RPC/超时回收；macOS/Termux 强杀恢复仍需目标平台运行验证 | `src/services/aria2/process.rs`、`README.md`、`deploy/SECURITY.md` |

验证结果：`cargo fmt --all -- --check`、`cargo test --all-targets --locked`（324 passed / 0 failed）、`npm --prefix web run typecheck` 和 `npm --prefix web run build:nocheck` 均通过。没有启动真实服务、浏览器、B 站、aria2、FFmpeg 或跨平台安装产物，因此动态和多平台结论仍按第 8 节边界处理。
