# B站视频监控助手 - 依赖列表
# 建议在虚拟环境中安装：python -m venv venv && source venv/bin/activate (Linux) 或 venv\Scripts\activate (Windows)
# 然后运行：pip install -r requirements.txt

flask>=2.3.0
flask-cors>=4.0.0
flask-sqlalchemy>=3.0.0
flask-socketio>=5.3.0
python-socketio>=5.9.0
python-engineio>=4.7.0
sqlalchemy>=2.0.0
apscheduler>=3.10.0
requests>=2.31.0
werkzeug>=2.3.0
